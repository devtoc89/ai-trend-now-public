export enum CrawlStatusEnum {
  CREATED = "created",
  IN_PROGRESS = "in-progress",
  COMPLETED = "completed",
  FAILED = "failed",
}

export enum PostCategoryEnum {
  PAPER = "paper",
  NEWS = "news",
}
